<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.roaming.ringo.visionroaming">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".CameraActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_camera"
            android:theme="@style/FullscreenTheme">

        </activity>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AnalyzeActivity"></activity>
        <activity android:name=".AnalyzeInDomainActivity"></activity>
        <activity android:name=".DescribeActivity"></activity>
        <activity android:name=".RecognizeActivity"></activity>
        <activity android:name=".ThumbnailActivity"></activity>
        <activity android:name=".helper.SelectImageActivity"></activity>
    </application>

</manifest>